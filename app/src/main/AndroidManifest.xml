<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dairy.dairyapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DairyApp">
        <activity android:name="com.dairy.dairyapp.activities.ProfileActivity"></activity>
        <activity android:name="com.dairy.dairyapp.activities.HomeActivity" />
        <activity
            android:name="com.dairy.dairyapp.activities.OnBordingActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name="com.dairy.dairyapp.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>